databases:
  - name: micronaut
    databaseName: micronaut_0fdn
    user: micronaut_0fdn_user

services:
  - type: web
    name: RoR_Recipe_Project
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: micronaut
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false